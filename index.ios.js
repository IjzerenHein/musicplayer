import {AppRegistry} from 'react-native';
import MainView from './app/src/MainView';

//Press Cmd+R to reload,{'\n'}
//Cmd+D or shake for dev menu

AppRegistry.registerComponent('musicplayer', () => MainView);